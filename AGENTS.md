# AGENTS.md

- 이 레포는 `interactive-state-flow` 스킬 리포다.
- 작업 맥락은 `$project-context` 기준으로 남긴다.
- shipped skill 계약은 `skills/interactive-state-flow/SKILL.md`에 둔다.
- `SKILL.md` 안에서 다른 스킬을 직접 참조하지 않는다.
- `SKILL.md`는 영어 기본 문서, `SKILL.ko.md`는 frontmatter 없는 한국어 페어로 유지한다.

